script:
  # Simulate zigbee2mqtt action
  - type: send
    at_ms: 100
    payload:
      type: event
      event: state_changed
      entity_id: sensor.magic_cube_action
      new_state: {state: "flip90"}
  - type: expect
    timeout_ms: 1000
    match:
      type: call_service
      domain: media_player
      service: toggle
      service_data: {entity_id: "media_player.living_room"}
